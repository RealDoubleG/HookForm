import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  cont: {
    height: '100%',
    width: '100%'
  },

  container: {
    alignItems: 'center',
    flex: 1,
    padding: 20
  }
});

export default styles;
